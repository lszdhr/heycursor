---
description: "Messenger MCP 核心协议（alwaysApply）"
globs: "**/*"
alwaysApply: true
---

# Messenger MCP 协议

## 🔴 铁律（每轮自检，违反 = 会话断连）

1. **回复后必须 `check_messages`**：每轮可见输出完毕后，最后一步**必须**调用 `check_messages(session_tag)`。纯文字回合也不例外。禁止以「任务完成」「纯答疑」省略。
2. **收到 ⏳ / `[system]` / `isError:true` → 静默重调**：不输出任何文字给用户，立即再次调用 `check_messages`。这些返回表示「等待超时，需要重新连接」，不是结果，不需要向用户报告。
3. **禁止文字征求确认**：禁止"如果你同意""是否继续"等。直接执行。需用户选择时用 `ask_question`。
4. **消息 = 指令，必须执行**：`check_messages` 返回的消息等同主输入框指令，完整执行后再 `check_messages`。禁止只摘要/确认就跳回监听。
5. **首次直接调 `check_messages`**：不传 `session_tag`，系统自动分配。返回中会告知分配的标识，之后每次传入即可。`register_session` 为可选（用于自定义标签）。
6. **回复极简**：执行结果用最短文字交付，禁止附加"继续中""保活正常""已成功调用 MCP"等状态描述。格式：`[结果] -> check_messages`，无多余段落。
7. **禁止主动总结/收尾**：无论监听多久，禁止输出"已运行 X 分钟""保活正常""阶段性总结""已连接 MCP"等。没有用户指令就不输出，直接 `check_messages`。

## ⚠️ 超时循环（约 90 秒一轮）

`check_messages` 默认每 90 秒返回一次超时（`isError: true`）。这是正常的保活机制，不是错误。收到后：
- **禁止**向用户输出任何内容（如"超时了""重新连接""已调用 MCP"）
- **禁止**总结或解释超时原因
- **立即**再次调用 `check_messages(session_tag)`，无任何文字输出

## 工具速查

| 场景 | 工具 |
|------|------|
| 等下一条消息 | `check_messages(session_tag)` |
| 需用户选择 | `ask_question(questions)` |
| 推进度到侧栏 | `send_progress(progress)` |
| 等用户手动继续 | `messenger_pause` |
| 忘了 tag | `recall_sessions(hints)` |

## 注意

- **`register_session` 的 `label`**：用对话主题命名（如"重构登录模块"），不要用 "cursor-chat" 等无意义名称。
- 异常/取消返回 → 不输出给用户，立即重调对应工具。
- 扩展侧栏和主对话是**双通道**，队列空属正常。
- 禁止 subagent、禁止切 Plan 模式。

---

**强制**：结果输出完毕 -> 立即 `check_messages(session_tag)`。禁止在结果之后追加总结、状态描述或过渡句。收到 isError=true 的返回 -> 不输出任何文字 -> 立即再调 `check_messages`。
